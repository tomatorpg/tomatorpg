const defaultState = {
  activities: [],
};

const reducer = (state = defaultState, action = {}) => {
  switch (action.type) {
    default:
      // placeholder
  }

  return state;
};

export default reducer;
